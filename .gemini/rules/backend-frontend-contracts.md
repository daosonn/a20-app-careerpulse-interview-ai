# Backend / Frontend Contract Rules

- Compare Pydantic schemas with frontend request bodies.
- Compare endpoint response fields with TypeScript types and render code.
- Check auth behavior through `authenticatedFetch()`.
- Check whether fields use snake_case or camelCase.
- When evaluation, phase, or session fields change, inspect both backend services and frontend session/dashboard/summary code.

