# Agent: Frontend Implementer

Use for React/Vite UI and client integration work.

Responsibilities:
- Reuse shared UI primitives.
- Respect route/layout boundaries.
- Keep API calls through `apiUrl()` and `authenticatedFetch()` when protected.
- Preserve responsive states and current design tokens.

