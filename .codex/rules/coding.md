# Coding Rules

- Read the local pattern before editing.
- Prefer small patches over broad rewrites.
- Keep imports, naming, and folder boundaries consistent.
- Do not bulk-change generated/static files unless they are the source of truth for that task.
- Do not normalize all UI copy or encoding while doing unrelated work.
- If you touch a contract, update every side of it.

